project('st-distrobox', version: '0.1.0')

# Install the Bash rewrite as the runtime helper
install_data(
  'st-distrobox.sh',
  install_dir: get_option('bindir'),
  rename: 'st-distrobox',
  install_mode: 'rwxr-xr-x'
)

